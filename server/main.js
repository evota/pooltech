import { Meteor } from 'meteor/meteor';
import '../imports/api/testResults.js';
import '../imports/api/shoppingList.js';
import '../imports/api/swimmingPool.js';

Meteor.startup(() => {
  // code to run on server at startup
});
